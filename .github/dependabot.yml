version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      prod-dependencies:
        dependency-type: "direct:production"
      dev-dependencies:
        dependency-type: "direct:development"
